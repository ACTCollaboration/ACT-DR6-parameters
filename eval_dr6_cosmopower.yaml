debug: false
force: false
timing: true

output: chains/dr6-evaluate-bestfit-dr4

sampler:
  evaluate:

theory: !defaults [theory_cosmopower]
likelihood: !defaults [likelihood_act_dr6]
params: !defaults [params_DR4_bestfit_H0, params_systematics_fixed]
prior: !defaults [priors_foregrounds, priors_cosmology]
